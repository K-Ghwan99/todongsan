spring:
  application:
    name: market-service
  profiles:
    default: local

server:
  port: 8081

mybatis:
  mapper-locations: classpath:/mapper/**/*.xml
  configuration:
    map-underscore-to-camel-case: true
